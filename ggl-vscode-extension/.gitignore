# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Optional: some prefer to commit this, others don't. For extensions, often not committed if using `npm ci` in builds.
                  # For now, let's ignore it to reduce file changes count.

# VSCode extension compiled output
out/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
