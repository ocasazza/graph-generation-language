# Automatically load the Nix flake development environment
use flake

# Optional: Set additional environment variables
export RUST_LOG=debug
export CARGO_INCREMENTAL=1

# Ensure we're using the flake's Python environment for docs
export PYTHONPATH=""
